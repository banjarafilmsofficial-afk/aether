[package]
name = "aether"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Aether — the universal control plane for AI resources, economics, agents, and execution."
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints.rust]
unsafe_code = "forbid"
